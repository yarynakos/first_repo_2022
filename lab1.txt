#include <stdio.h>
#include <math.h>

int main() {

    float x = 0.069;
    float y = 3.017;
    float res = 137 * pow(x, 3) + cos(pow(y, 3) / pow(x, 4)) + tan(14 * y) - 7 * pow(x, 6);
    printf("%f", res);
kj;
    return 0;
}
